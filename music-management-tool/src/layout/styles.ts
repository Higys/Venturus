import styled from "styled-components";

export const LayoutContainer = styled.div`
    max-width: 95rem;
    height: calc(87vh);
    padding: 1rem;
    margin: 5px auto;

    display: flex;
    flex-direction: column;
`;